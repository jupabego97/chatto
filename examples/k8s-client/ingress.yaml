# Requires an Ingress controller (Traefik) and cert-manager
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: redirect-https
  namespace: chatto-client
spec:
  redirectScheme:
    scheme: https
    permanent: true
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: chatto-client
  namespace: chatto-client
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    traefik.ingress.kubernetes.io/router.middlewares: chatto-client-redirect-https@kubernetescrd
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - chatto.cc
      secretName: chatto-client-tls
  rules:
    - host: chatto.cc
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: chatto-client
                port:
                  number: 80
