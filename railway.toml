[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Healthcheck disabled until the service boots cleanly; re-enable /healthz after
# deploy logs show "Starting HTTP server".
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
