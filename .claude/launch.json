{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "chatto",
      "runtimeExecutable": "bash",
      "runtimeArgs": [
        "-c",
        "COMPOSE_PROJECT_NAME=\"$(basename \"$PWD\")\" exec docker compose up --build --remove-orphans"
      ]
    }
  ]
}
